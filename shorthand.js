// *Variable declaration
var tomatoPrice = 25;

// *Variable value update 
tomatoPrice = 30;

// *Add something to the previous value
var price1 = 31;
var price2 = 10;

// *Set a new value
// *price1 = 35;

// *Add some value to the existing value
price1 = price1 + 10;

console.log(price1);

// *addition to the same variable shorthand
// * += means adding some value to the existing values 
price1 += 10;
console.log(price1);

price2 = price2 - 5;
//* shorthand -=
// * -= means deducting some values from the current value of the variable.
price2 -= 5;

// * *=

price1 *=5;
console.log(price1);
// * /=

price1 /= 5;
console.log(price1);

var age = 25;

age = age + 1;
// * ++ means adding 1 to the existing
age++;

var money = 100;

money = money + 1;
// * -- means substracting 1 to the existing

money--;
money--;
console.log(money);